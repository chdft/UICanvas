<UserControl x:Name="Control"
			 x:Class="UICanvas.CanvasControl"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:local="clr-namespace:UICanvas"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 mc:Ignorable="d"
			 d:DesignHeight="300"
			 d:DesignWidth="300">
	<ItemsControl ItemsSource="{Binding ElementName=Control, Path=Screens}">
		<ItemsControl.Resources>
			<Style TargetType="ContentPresenter">
				<Setter Property="Canvas.Top"
						Value="{Binding Path=YOffset}" />
				<Setter Property="Canvas.Left"
						Value="{Binding Path=XOffset}" />
			</Style>
		</ItemsControl.Resources>
		<ItemsControl.ItemsPanel>
			<ItemsPanelTemplate>
				<Canvas Background="#FF969696"></Canvas>
			</ItemsPanelTemplate>
		</ItemsControl.ItemsPanel>
		<ItemsControl.ItemTemplate>
			<DataTemplate>
				<local:ScreenControl Screen="{Binding}"></local:ScreenControl>
			</DataTemplate>
		</ItemsControl.ItemTemplate>
	</ItemsControl>
</UserControl>
